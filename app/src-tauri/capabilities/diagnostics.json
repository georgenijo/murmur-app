{
  "$schema": "https://schemas.tauri.app/config/2/capability",
  "identifier": "diagnostics",
  "description": "Capability for the pop-out Diagnostics window",
  "windows": ["diagnostics"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging"
  ]
}
